name: flutter_map_marker_popup2
description: Marker popups for flutter_map.
version: 7.0.0
homepage: https://github.com/tom2you/flutter_map_marker_popup2
topics:
  - flutter-map
  - popups

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  animated_stack_widget: ^0.0.4
  flutter_map: ^7.0.0
  latlong2: ^0.9.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
